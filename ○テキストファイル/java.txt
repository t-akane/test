○java

eclipse　環境を使用
このPCは64bit
OS7

コンパイル　人間用を機械用に変換すること
javaのプログラムは、プロジェクトと呼ばれるワンセット単位で管理する。

・今までと違う注意点
記述の途中で開業するとエラーになる。
<br>がなくても行を分けて記述すれば改行される。

改行コードは<br>じゃなくて\r\n

・変数
変数は使う前に、型と名前の定義が必要。
int A; など
大文字と小文字は区別する。数字だけ、数字から始まるのはNG

<プリミティブ型一覧>
boolean 真偽値を格納

char 16bitのunicode文字
byte　8bitの符号付整数（-128～127）
short　16bitの符号付整数（-32768～32767）
int　32bitの符号付整数
long　64bitの符号付整数

float　32bitの単精度浮動小数点数
double　64bitの倍精度浮動小数点数

文字列を格納できる型はないのでStringクラスを参照する（？）
String name="田中";
System.out.println(name);


・四則演算
割り算は基本的に商のみ表示、あまりは表示無し。
小数点以下を求めたい場合は、浮動小数点型を使用する。


・加算子（だいたい今までと一緒）

前置加算
++a　元の値に1を加えたものを表示。
a++　元の値を表示し、1加える。

だいたいいままでと一緒

・論理演算子
① && ② ともにTRUE
① || ②　少なくともどちらかTRUE
! ①　　①がTRUEの時、処理は実行しない

①②には条件式など

・if（だいたい今までと一緒）
if(条件){処理内容;}

・elseif
if(条件){処理内容;}
 elseif(条件){処理内容;}
上の条件でfalseだった場合のうち、もしその行のifがtrueだったら実行。
elseifにもfalseだった場合は、何も起こらない。

・else
if(条件){処理内容;}
 else(条件){処理内容;}
上の条件でfalseだった場合すべて。


・ネスト（入れ子）

ifがtrue→そのなかにもif文
複数の条件に合致する場合を設定したいとき

かつ、を設定したい場合はif文の中にさらに条件式を設定する。


・switch文　切り替え
a==bという等価式に特化した条件分岐
等価式を複数用意していずれかに当てはまるときに実行する。

switch(a)
{case*:
処理内容;
break;

default 処理内容;}

主語がaの部分
もし＊＊ならがcaseの後
break;はお約束
defaultはいずれの等価式にも当てはまらなかった場合に実行される

・for（大体一緒）
for(初期値;条件式;増減式){処理内容;}

・条件演算子
条件式 ? 式1 : 式2
条件式がTRUEの場合、式１を実行する。
FALSEの場合、式２を実行する。

例
int aa=10;
String bb = aa>=0 ? "プラス":"マイナス";  
System.out.println(bb);

・while文（だいたい一緒）
初期値;
while(条件式)
{処理内容;増減式;}

●do while(new!)
初期値;
do{処理内容;増減式;}while(条件式);


・配列
変数に共通の土台を追加する

箱の通し番号はkey　添え字
箱の中の値を値　value
箱と値をあわせて　element　要素
通し番号に入れる、上書きする、削除する、先頭や末尾に追加　など

String[]name={"田中","高橋","山田"}

多次元配列
String[][]country={
{"日本","タイ"},{・・・}
};
この場合
[0][0]→日本
[0][1]→タイ

多次元配列2
String[][]country=new String[3][2];
countory[0][0]="日本";
countory[0][1]="タイ";
・
・
・



ー-------以下はPHPのときのメモ----------



・連想配列　多次元配列
連想配列とは
箱の通し番号key0.1.2を違う番号にして管理する方法

$country=array("アジア"=>"日本","アメリカ"=>"アメリカ","欧州"=>"フランス");
アジア、アメリカ、欧州がそれぞれはこの名前になった

多次元配列とは
箱の中にさらに小さな箱を増やす
$counry=array
array("日本","タイ"),
array("アメリカ","ブラジル"),
array("フランス","ロシア");

上の二つを合わせる

$counry=array
"アジア"=>array("日本","タイ"),
array("アメリカ","ブラジル"),
array("フランス","ロシア");

valueはすでに箱なのかもしれない。

・シングルとダブルクオーテーションの違い
基本は同じ。文字列を挟んだり計算式をはさんだりする場合は同じ。
囲むものの中に変数が含まれていた場合
シングル→展開しない
ダブル→展開する
という違いがある。

ドットは文字列とそれ以外のつなぎ。前にも後ろにも必要。
echo $a."さん":

$name=""+
$age="";
echo $name."さん(".$age."歳)";
↓
山田さん(25歳)

・コメントアウト
//をつけたあとは実行されない。その行のみ有効。
複数の行にまたがるときは/*と*/ではさむ。

・forとwhileは割愛

・foreachフォーイーチ
それぞれを実行していく

$a=array("山田","鈴木","佐藤");

foreach($a as $b=>$c){echo $b.$c;}
↓
0山田1鈴木2佐藤
=>というのは箱とその中身

・ソート
普通に表示させると格納した順
sort昇順
rsort降順
ksort　keyの昇順
kesort　keyの降順
asort　値の昇順
arsort　値の降順





























































